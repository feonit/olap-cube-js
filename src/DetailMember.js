import Member from './Member.js'

/**
 * Detailed item. The dimension element, which is its lower hierarchical level
 * */
export default class DetailMember extends Member {
    constructor(options){
        super(options)
    }
}